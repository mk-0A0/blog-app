mutation UpdatePost($input: UpdatePostInput!, $uuid: String!) {
    updatePost(input: $input, uuid: $uuid) {
        content
        title
        categories {
            id
            name
            uuid
        }
    }
}
