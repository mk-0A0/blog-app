query PostDetail($uuid: String!) {
    post(uuid: $uuid) {
        uuid
        content
        title
        categories {
            id
            name
            uuid
        }
        author {
            email
            name
        }
    }
}
